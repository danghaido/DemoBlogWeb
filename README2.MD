## Những việc cần phải làm khi muốn chạy file server.js thì phải tạo các lệnh sau: 

# Tạo package.json tự động
npm init -y     

# Cài đặt các gói cần thiết
npm install express
npm install cors
npm install body-parser

# Chạy server
node server.js